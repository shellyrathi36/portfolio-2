# Shelly Rathi Portfolio

A self-contained, responsive portfolio built with semantic HTML, CSS, and vanilla JavaScript. It uses an olive-green visual system, subtle orbital motion, scroll reveals, animated metrics, and a reduced-motion fallback.

## Run locally

From this directory, run:

```bash
python3 -m http.server 8000
```

Then open <http://localhost:8000>.

